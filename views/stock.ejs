<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <%- include("./partials/bootstrap") %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Data</title>
</head>
<body>
    <br><br>
    <div class="col-md-3 mb-3">
        <a type="button" class="btn btn-info" href='#' role="button">Back</a><br>
    </div>

    <div class="col-md-3 mb-3">
        <h1>Product</h1>
        <h1><%= title[0] %></h1>
        <h4>Category <%= title[1] %></h4>
    </div>

    <div class="card">
        <div class="card-body">
          <table class="table table-hover">
            <thead class="table table-bordered">
              <tr class="table-primary">
                <th scope="col">Warehouse</th>
                <th scope="col">Stock</th>
              </tr>
            </thead>
            <tbody class="table table-bordered">
                <% if(result.length !== 0){for (let i = 0; i < result.length; i++) { %>
                    <tr>
                        <td><%= result[i].Warehouse.getWarehouseName() %></td>
                        <td><%= result[i].Stock %></td>
                    </tr>
                <% }} else{%>
                        <td colspan="4"><h1 class="text-center">Tidak ada Warehouse terdaftar untuk produk ini</h1></td>
                <% } %>
            </tbody>
          </table>

          <a type="button" class="btn btn-danger" href='/products/<%=title[2]%>/addstock' role="button">Tambah Stock Warehouse</a>

        </div>
    </div>
</body>
</html>